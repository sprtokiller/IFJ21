
-- god comment

require "ifj21"

global x: function(string):number, boolean
global x: number
global x: boolean = true

function main():string, boolean
	if a>b then
		return c
	elseif b>c then
		if a<c then
			return x
		end
	else 
		print("no")
	end
end
