require "ifj21"

global x:function():integer
global x:number = x()

function main():string, boolean
	if a>b then
		return c
	elseif b>c then
		if a<c then
			return x
		end
	else 
		print("no")
	end
end
